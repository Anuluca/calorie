{
  "name": "calorie-ai-demo",
  "private": true,
  "workspaces": [
    "mobile",
    "worker"
  ],
  "scripts": {
    "dev": "npm run dev --workspace mobile",
    "build": "npm run build --workspace mobile && npm run check --workspace worker",
    "test": "npm run test --workspace mobile && npm run test --workspace worker",
    "native:sync": "npm run native:sync --workspace mobile",
    "worker:dev": "npm run dev --workspace worker",
    "worker:deploy": "npm run deploy --workspace worker"
  },
  "engines": {
    "node": ">=22"
  }
}

