{
  "name": "calorie-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply calorie-foods --local",
    "db:migrate:remote": "wrangler d1 migrations apply calorie-foods --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260820.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "wrangler": "^4.124.0"
  },
  "dependencies": {
    "zod": "^4.1.12"
  }
}
